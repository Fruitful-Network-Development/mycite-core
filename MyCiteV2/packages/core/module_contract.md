# Core Module Contract

- Owns: pure deterministic logic with no runtime, adapter, tool, sandbox, or host dependency.
- Does not own: shell legality, domain semantics, ports, adapters, tools, sandboxes, or runtime composition.
- Consult: `docs/ontology/structural_invariants.md`, `docs/ontology/dependency_direction.md`, `docs/plans/phases/02_core_pure_modules.md`.
