# Modules

Authority: [../../../docs/plans/v2-authority_stack.md](../../../docs/plans/v2-authority_stack.md)

`packages/modules/` is the semantic owner layer. It is split deliberately into `domains/` and `cross_domain/` to prevent broad service-style dumping.
