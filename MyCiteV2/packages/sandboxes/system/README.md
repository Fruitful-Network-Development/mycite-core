# System Sandbox

Status: reserved stub (not yet implemented).

Placeholder for system-scoped orchestration boundaries only. It will host the
system sandbox runtime — the privileged sandbox that hosts system-level surfaces
distinct from grantee sandboxes.

Design spec: [`20-l2-surface-persistence.md`](../../../../docs/wiki/20-l2-surface-persistence.md)
(forward reference — this wiki page is produced by a sibling unit in the current
documentation batch and may not be present on `main` yet).
