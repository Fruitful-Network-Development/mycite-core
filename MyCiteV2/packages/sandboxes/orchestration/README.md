# Sandbox Orchestration

Status: reserved stub (not yet implemented).

Placeholder for shared sandbox orchestration helpers that do not own domain
semantics. Today sandbox identity lives implicitly in the document-id and
`tool_id`; this package will host the explicit orchestration that coordinates
sandboxes once that identity is lifted out.

Design spec: [`20-l2-surface-persistence.md`](../../../../docs/wiki/20-l2-surface-persistence.md)
(forward reference — this wiki page is produced by a sibling unit in the current
documentation batch and may not be present on `main` yet).
