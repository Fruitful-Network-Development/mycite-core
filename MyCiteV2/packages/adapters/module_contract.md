# Adapters Contract

- Owns: concrete port implementations only.
- Does not own: domain semantics, shell truth, or host composition policy.
- Consult: `docs/contracts/import_rules.md`, `docs/plans/phases/06_adapters.md`.
