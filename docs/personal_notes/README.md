# Personal Notes

Personal notes were reset to match the one-shell portal model. Any notes that
encoded the retired split-shell assumptions were removed.

Archived MOS source-only notes now live under `docs/personal_notes/archive/MOS/` so the active note path stays limited to material that still supports current repo work.
