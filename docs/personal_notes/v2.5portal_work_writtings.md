# TITLE

## Portal Shell Hardening and Chrome Simplification

```text
Now I want you to consider the images attached, my request of you is to identify the individual visual components and behaviors that I note below. I want you to audit the docs, code, and untimely update the deployed portal. The audit should address the surrounding areas, behaviors and terminology based on my notes so that the UI and portal is made to be more simple and stable in its modular form, done by considering the nature of the particular points of interest as indacators for other issues that may be similarly present and that can similarly be prevented with better hardy simple design code and documentation. 
(See the apliacble information for auditting and correction in `/srv/repo/mycite-core/`)

1.) Upon loading any of the pages, each one has an initial page before idle back to the content that reads "Shell hydration failed" and "The portal shell bundle did not load or did not execute.".
- On first render, the shell is briefly showing the fatal shell-boot path: the workbench card titled Shell hydration failed, with the message The portal shell bundle did not load or did not execute. That text is emitted by patchFatalState / showFatal before normal hydrated state is applied.

2.) There is a duplicate menu bar header on each page, seemingly inside of the area below the menu bar.
- The template renders both the top Menu bar (ide-menubar) and a workbench pagehead (pagehead pagehead--withTools) inside the ide-workbench. The pagehead is the second header region below the Menu bar.

3.) The duplicate  header menu bar only features the duplicate information of the page title, subheading, and theme section. The theme selector in this duplicate header menu bar works, but the theme selector in the menu bar (the one that should exist alone), does not work. 
- The duplicated fields are the shell page_title, page_subtitle, and the Portal theme selector. In the template, both the ide-menubar__theme control and the pagehead__theme control are [data-theme-selector] pickers, and initThemeSelector() is written to synchronize them.

4.) In the real Menu bar there should exist a interface panel toggle icon button to the right of a working theme selector, instead, on some pages, the interface panel is present in the initial view on default, it is also blank and at a minimum should be labeled similar to the control panel.
- The top-right shell action is the Interface Panel button under Shell layout controls, and it targets portalInspector, whose shell-region name is interface-panel. In the template, the interface panel starts is-collapsed. When it has no mounted content, its empty-state copy is Select an item to load interface panel content. For tool surfaces, the shell posture treats the interface panel as open/primary by default.

5.) The control panel does not appropriately contain the information held. The information is expanded to touch either side of the control panel with out a margin.
- The control-panel spacing issue is specifically that kind="focus_selection_panel" is rendered directly as ide-controlpanel__selectionPanel, instead of being wrapped in the standard ide-controlpanel__section. That bypasses the normal section padding used by the non-selection control-panel renderer.

6.) The control panel is not uniformly populated. it should read the the current focus and depth position of the portal. Starting with the sandbox, followed by situational applicable additions of the document, the datum.
- The repo term for the control panel’s focus/depth rows is context_items. In SYSTEM, those context_items are built from Sandbox, File, and, when applicable, Datum, Object, or Mediation. The canonical focus-stack terms are sandbox, file, datum, and object.

7.) The control panel should have sections stacked from the bottom ordered by convention. First would be the control panel title. Second would be the page title section. Third would the attention (focus depth hierarchy). Forth would be details about the object of focus (For a datum this would be the details of the datum likes its abstraction path, etc. for a file this would be the layers, for a layer this would be the value groups, for value groups this would be datums, for a sandbox this would be )
- The repo-defined stacked order for a focus_selection_panel is: selectionHeader (title + surface_label), then context_items, then verb_tabs, then groups, then actions. The selectable material below the current context is carried by groups. I do not see a separate generic repo field called “details about the object of focus”; that content is represented either as groups in the control panel or as inspector sections such as Current focus.
```

### PLAN

**Summary**
- Fix the false `Shell hydration failed` flash by making shell boot/fatal handling match real bundle state instead of missing/implicit flags.
- Remove the duplicate workbench header so page title, subtitle, theme, and shell controls live in the top menubar only.
- Normalize shell terminology and posture: `Control Panel` on the left, `Interface Panel` on the right, with the right panel closed by default except on tool-led surfaces or explicit selection states.
- Keep `UTILITIES` simple: no fake sandbox/file/datum depth, but give it the same modular control-panel shell with minimal context plus grouped section links.

**Implementation Changes**
- Shell/template/static asset contract:
  - Update [portal.html](/srv/repo/mycite-core/MyCiteV2/instances/_shared/portal_host/templates/portal.html) so the workbench is content-only; remove `pagehead pagehead--withTools` and keep a single menubar theme selector.
  - Rename the left toggle from `Context` to `Control Panel`.
  - Keep the `Interface Panel` control in the menubar as the far-right icon-style action with stable `aria-label`/title.
  - Add `portal_build_id` cache-busting to `portal.css`, `portal.js`, `v2_portal_shell.js`, and the internal V2 shell modules loaded by `v2_portal_shell.js`.
  - Expose one canonical shell asset manifest so template output, JS module loading, and host health checks cannot drift.
- Shell boot and panel state:
  - In `v2_portal_shell_core.js`, set canonical globals for `core_loaded`, `hydrated`, and fatal handling, and export one fatal entrypoint.
  - Make the watchdog bundle-delivery-only; do not show fatal UI for normal shell POST latency once the core has loaded.
  - Change composition semantics so `inspector_collapsed` defaults to `true` unless `regions.inspector.visible === true` or the surface is explicitly tool-primary.
  - Ensure the inspector renderer always shows labeled empty-state copy instead of a blank white panel when no detail body is mounted.
- Control-panel rendering and surface behavior:
  - Wrap `focus_selection_panel` output in the standard `.ide-controlpanel__section` container so spacing matches the rest of the shell.
  - Preserve SYSTEM and SYSTEM-tool context rows as `Sandbox`, `File`, then contextual `Datum`, `Object`, or `Mediation`.
  - Keep NETWORK on `Root/View/Contract/Type`, but collapse its interface panel until a log record is selected; selecting a record reopens it.
  - Convert UTILITIES from `plain_navigation` to a minimal selection-panel shape: `Control Panel / UTILITIES` header, minimal root/current-section context only, no fake depth rows, no verb tabs, and one grouped `Sections` list for current utilities children.
  - Do not add new Utilities routes in this pass; keep the panel ready for future sections like vault/tools/config/peripherals when those surfaces actually exist.
- Docs and terminology:
  - Update the shell contract and surface catalog docs to state that the menubar is the only shell header, the left rail is the `Control Panel`, the right rail is the `Interface Panel`, NETWORK opens the interface panel only on selected-record focus, and UTILITIES is section-led rather than focus-depth-led.
  - Add or update one audit note documenting the hydration-flash root cause, inspector-collapse contract, control-panel padding fix, and asset-manifest/cache-busting change.

**Test Plan**
- Add runtime/unit coverage for:
  - `system.root` returning `inspector_collapsed=true` during ordinary navigation.
  - `utilities.root` returning `inspector_collapsed=true` and the new minimal utilities control-panel model.
  - `network.root` with no `record` returning `inspector_collapsed=true`, and with a `record` returning `inspector_collapsed=false`.
- Add static/template/architecture coverage for:
  - exactly one `data-theme-selector` in rendered HTML,
  - absence of `pagehead--withTools`,
  - build-id versioned asset URLs,
  - parity between the health/static manifest and the JS loader’s internal module list.
- Re-run:
  - `python3 -m unittest MyCiteV2.tests.unit.test_portal_shell_contract MyCiteV2.tests.unit.test_portal_workspace_runtime_behavior MyCiteV2.tests.integration.test_portal_host_one_shell MyCiteV2.tests.contracts.test_contract_docs_alignment`
- Post-deploy smoke on both services:
  - `GET http://127.0.0.1:6101/portal/healthz` and `:6203` show the new build id.
  - `GET /portal/system` loopback HTML shows one header and one theme selector.
  - Shell POST smoke confirms corrected `inspector_collapsed` states for `system.root`, `network.root`, and `utilities.root`.
  - Browser verification on `/portal/system`, `/portal/network`, `/portal/utilities`, and `/portal/system/tools/aws-csm`: no hydration-fatal flash, no duplicate header, working menubar theme control, no blank-open right panel on root pages, tool pages still remain interface-panel-led.

**Public Interface / Contract Notes**
- No route or schema version changes.
- Behavioral contract changes:
  - the top menubar is the only page-title/theme header,
  - `shell_composition.inspector_collapsed` now truthfully reflects right-panel state,
  - UTILITIES becomes standardized but stays section-led rather than focus-depth-led.

**Assumptions**
- Deploy scope is both `mycite-v2-fnd-portal.service` and `mycite-v2-tff-portal.service` because the shared shell assets and host code are common.
- This pass does not introduce new Utilities pages such as vault/config/peripheral routes.
- `Control Panel` is the canonical user-facing term everywhere the left rail or its toggle is named.

---


## Portal Shell Peer-Region Normalization

```text
In the same fashion as done prior, here above in the last prompt-plan-implementation, consider these points below:

1.) The interface panel, upon toggling its presents, shifts the work bench over as an equal level window.
- Opening the Interface Panel changes the ide-body shell grid by adding the inspector region and its splitter beside the workbench; in repo terms, the Interface Panel is a peer shell region, not an overlay, except under the responsive mobile posture.

2.) The top level objects are the UI window and the menu bar.
- The top-level shell structure is ide-shell, and it is divided into ide-menubar and ide-body. The repo contract also now states that the top menubar is the only shell header.

3.) The activity bar, the control panel, the workbench and the interface panel are equal level areas and can shift one another over or be resized.
- Within ide-body, the peer shell regions are the Activity bar, the Control Panel, the workbench, and the Interface Panel. More precisely, they are the activity, controlpanel, workbench, and inspector grid areas. Only the Control Panel and Interface Panel have explicit splitters and persisted width state in the shell layout code.

4.) The work bench is only defaulted to be toggled on  for the System page, Network page, Utilities Page. On those pages, by default, the interface panel is toggled off. On the tool pages the work bench, by default is toggled off and the interface panel is toggled on.
- The root-surface default posture is workbench-primary with the Interface Panel collapsed by default. The contract names SYSTEM, NETWORK, and UTILITIES as root workspaces, and each keeps the Interface Panel collapsed until mediation or explicit projected detail exists. Tool work pages under SYSTEM are instead interface-panel-led; the tool contract states that every tool registry entry defaults to interface_panel_primary, and every tool composition defaults to regions.workbench.visible=false.

5.) One a given tool page, the interface panel, defaulted to be present, will expand to take up the remaining space to sit beside the control panel.
- On a tool surface, the precise repo wording is that the tool posture is interface-panel-led, with the Interface Panel as the primary_surface in dominant layout, while workbench visibility defaults to false. I would avoid saying it “takes the remaining space,” because the code more specifically describes an expanded dominant inspector posture rather than a full takeover statement.

6.) The interface panel by default is reserved for state machine directives that reflect the intention of state. Therefore, tools are not defaulted to assume any other directive state other than mediation. The state machine then conventionally mediates on the anchor file with respect to supporting files or sources used.
- The grounded repo statement is: the Interface Panel is mediation-owned on system.root, and verb=mediate opens it and binds it to the current mediation subject. For tool surfaces, the contract is interface-panel-led, not generic workbench-led. I do not see repo language that defines the Interface Panel generically as a container for “state machine directives”; the repo’s explicit terms are mediate, mediation subject, interface_panel_primary, and file-backed tool context.

7.) Anchor files are the conventional core of a sandbox and are what allow datum files to contain datum abstraction that act as 'branches' by creating datum that may assume layers are the same as the anchor file, and therefore can define datums for addresses that may already exist in the anchor file, but for different abstraction. (Seeing as how iteration values of the datum address merely increment by the order they are added, and are still uniquely understood by there hyphae value)
- The precise anchor-file wording supported by the repo is narrower than your draft. Use this instead: the contract-level anchor-file invariant is that a fresh reducer-owned SYSTEM entry seeds file=anthology; anthology is the canonical system anchor file and default datum-file workbench; it is rendered as a layered datum table grouped by layer and value_group; datum rows carry structural coordinates layer, value_group, and iteration. I would avoid the stronger terms branches, “same as the anchor file,” and hyphae value here, because I do not see those shell-contract claims in the repo files I checked.

8.) Mediation views for the dominant interface panel are toggled off if the work bench is toggled on. Avoid having them both on for tools.
- The repo-supported statement is: tools default to an interface-panel-led posture, and workbench visibility on tool compositions defaults to false; any tool workbench content is explicit opt-in and treated as secondary-evidence workbench content. I do not see a repo-wide rule that says “turning on the workbench must turn off the Interface Panel,” so I would avoid stating that as a hard invariant unless you add it to the contract.


Notice that in these notes and those above that the technical descriptions differ from my use of terms, there in I want you to also audit the terminology in the code, and documentation for normalizing terms.

The audit of these problem/problem areas and the terminology normalization should be addressing them systematically, resulting in a consolidated, more sturdy, and a more well polished modular application design.
```

### PLAN

- Normalize the shell around one public vocabulary: `ide-shell` split into `ide-menubar` and `ide-body`, with peer `Activity Bar`, `Control Panel`, `Workbench`, and `Interface Panel` regions.
- Keep compatibility by adding `interface_panel` aliases instead of breaking `inspector` internals in one pass.
- Make posture consistent and explicit: `SYSTEM`, `NETWORK`, and `UTILITIES` are workbench-primary by default; tool surfaces are interface-panel-led by default; `AWS-CSM` is brought into that same tool default.
- Add a first-class `Workbench` menubar toggle, but keep resizing explicit only for `Control Panel` and `Interface Panel`.

**Implementation Changes**
- **Contract and terminology audit**
  - Update the shell docs and audit notes so the top menubar is the only shell header, `ide-body` is the peer-region window, and the right rail is always called `Interface Panel` in public UI/docs.
  - Reserve `inspector` for compatibility notes only. Where docs currently say “inspector lens” inside the workbench, rename that wording to `detail lens`, `selection detail`, or `read-only detail` so it is not confused with the shell’s right rail.
  - Normalize the anchor-file language to the repo-backed invariant only: fresh reducer-owned `SYSTEM` seeds `file=anthology`; `anthology.json` is the canonical system anchor file; it renders as a layered datum table grouped by `layer` and `value_group`; rows carry `layer`, `value_group`, and `iteration`.
  - Replace leftover public-facing `Context` and stale duplicate-header terminology in comments, CSS notes, template fallback text, tests, and docs with `Control Panel` / `Interface Panel` / `Workbench`.

- **Runtime and payload normalization**
  - Keep existing schema ids and existing `inspector` keys working.
  - Add additive aliases in the shell composition payload:
    - `interface_panel_collapsed` mirroring `inspector_collapsed`
    - `workbench_collapsed` derived from `regions.workbench.visible`
    - `regions.interface_panel` mirroring `regions.inspector`
  - Keep `regions.inspector`, `inspector_collapsed`, `primary_surface`, and `layout_mode` as compatibility fields in the same payload version.
  - Centralize default posture rules in the shell composition builder:
    - `SYSTEM`, `NETWORK`, `UTILITIES`: `workbench.visible=true`, interface panel collapsed by default
    - `SYSTEM` mediation: interface panel opens only from `verb=mediate` or explicit projected detail
    - tool surfaces: `workbench.visible=false`, interface panel open, `primary_surface=true`, `foreground_shell_region="interface-panel"`
  - Align `AWS-CSM` to that tool default so it no longer ships as a visible-by-default workbench exception.
  - Add explicit compatibility comments stating that `inspector` is the legacy alias for the public `Interface Panel`.

- **Shell layout and client behavior**
  - Add a menubar `Workbench` toggle alongside the existing shell controls.
  - Introduce `data-workbench-collapsed` on the shell root and make the client layout code sync `Control Panel`, `Workbench`, and `Interface Panel` posture together.
  - Workbench toggle behavior:
    - root surfaces default to workbench open
    - tool surfaces default to workbench closed
    - the toggle is safe, not destructive: it only hides the workbench when another primary region is visible
    - on root surfaces with a collapsed interface panel, the workbench toggle is disabled rather than allowing a blank center region
    - on tool surfaces, toggling the workbench on does not auto-close the interface panel
  - Keep explicit resize persistence only for `Control Panel` and `Interface Panel`.
  - Add new client aliases while preserving old ones:
    - read old `mycite.layout.inspector.*` keys but start writing `mycite.layout.interface_panel.*`
    - keep existing inspector events working, but add matching `interface-panel` event aliases
    - keep existing DOM ids/classes readable, but add interface-panel-oriented helper names and data attributes
  - Update the grid behavior so a dominant tool interface panel fills the remaining central space only when the workbench is collapsed; when the workbench is toggled back on, the layout returns to a peer two-surface posture.

- **Surface-specific cleanup**
  - Bring `AWS-CSM` into the shared tool posture, with its domain gallery as secondary workbench content revealed by the new Workbench toggle.
  - Keep `CTS-GIS` and `FND-EBI` interface-panel-led by default and give their hidden workbench state a clear secondary-evidence empty or readiness message when revealed.
  - Keep `NETWORK` and `UTILITIES` workbench-primary by default, with interface detail opening only from selected-record or explicit projected detail state.

**Public Interfaces**
- No route changes and no schema version bump.
- Additive public payload changes:
  - `shell_composition.interface_panel_collapsed`
  - `shell_composition.workbench_collapsed`
  - `shell_composition.regions.interface_panel`
- Compatibility expectations:
  - `shell_composition.inspector_collapsed` and `shell_composition.regions.inspector` remain valid
  - legacy `inspector` storage keys and JS events continue to work during the compatibility phase
- Public terminology standard:
  - `Control Panel` = left rail
  - `Workbench` = center content region
  - `Interface Panel` = right rail
  - `inspector` = compatibility alias only, not the preferred public term

**Test Plan**
- Add runtime/unit coverage for posture defaults and alias parity:
  - `system.root`, `network.root`, and `utilities.root` return `workbench_collapsed=false`, `interface_panel_collapsed=true`
  - tool surfaces return `workbench_collapsed=true`, `interface_panel_collapsed=false`
  - `AWS-CSM` now matches the shared tool default
  - `regions.interface_panel` exactly mirrors `regions.inspector`
- Add host/template/static tests for:
  - one shell header only
  - presence of `Workbench`, `Control Panel`, and `Interface Panel` shell controls
  - new `data-workbench-collapsed` and interface-panel alias behavior
  - layout JS reading old inspector storage keys and writing new interface-panel keys
  - CSS grid rules for workbench-collapsed dominant tool posture
- Add docs/contract tests to ensure:
  - public docs prefer `Interface Panel` over `inspector` for shell chrome
  - anchor-file wording matches the narrowed contract
  - workbench-local detail wording no longer collides with shell interface-panel terminology
  - tool posture docs state that coexistence is allowed when secondary workbench content is explicitly projected

**Assumptions & Defaults**
- This pass is compatibility-preserving, not a breaking rename.
- `inspector` remains supported internally and in public payloads until a later deliberate schema revision.
- The new Workbench control is toggle-only; this pass does not add a third resize handle.
- `Control Panel` and `Interface Panel` remain the only explicitly resizable shell regions.
- `AWS-CSM` should be normalized to the same default interface-panel-led posture as the other tool surfaces, not retained as a visible-workbench exception.

---


## CTS-GIS Tool Language and Interface-Body Unification

> CTS-GIS is the system.tools.cts_gis tool_mediation_surface under SYSTEM. Its default posture is interface-panel-led: the dominant Interface Panel is the primary tool surface, and the workbench remains tool_secondary_evidence until explicitly requested. The CTS-GIS tool_mediation_panel mounts one CTS-GIS-local interface body with two tool sections: Diktataograph and Garland. CTS-GIS mediates on the selected anchor-file datum and projects correlated source-file evidence into the Interface Panel. The Diktataograph section is the structural navigation section for the active SAMRAS-defined address space. It projects navigable structure addresses from the active mediation subject and may overlay correlated labels through the ASCII lens. The Garland section is the correlated profile section. It projects the current address node’s profile and spatial evidence with respect to the supporting source file, preferably through the GeoJSON lens or equivalent runtime cache. CTS-GIS seeds its tool-local structural navigation from anchor-file datum 1-1-2 and defaults its tool-local navigation root to structure address 3-2-3-17-77, corresponding to summit_county. Diktataograph updates the current tool-local navigation root; Garland updates the correlated profile projection for that root. These are not separate mediation operations; they are two CTS-GIS-local projections of one active mediation posture.

```text
Now I want to fully unify tool language completely and do other ground work alignment to make development of tool less complex and more stable. Specifically addressing how to populate the interface panel's content for the CTS-GIS tool.
- I wish to articulate a two pane section window.
- Since the tool will be showing this in a full interface panel context it can assume a 'stack' approach that has two section side by side. (If there needs to be denotation of how to display in narrow interface panel, then a 3 section vertical stack can be defined for this case.)
- Call the left section the 'diktataograph' window and the right section the 'garland' window.
Now I want to articulate how tools operate when they aren't just a mediation of the sandbox. Seeing as how the CTS-GIS is specifically a view that mediates on the defined SAMRAS strucure in the anchor file, this is meant allow for a visual navigation and section with in defined nodes, all with respect to the the source file, `sc.3-2-3-17-77-1-6-4-1-4.msn-administrative.json`. This allows for the top left section to show that visual navigation of node addresses, including names for those that are defined in the source files (making use of the ascii lens).
This should allow for a visual navigation of the structure, importantly distinct from mediation on the source file. Since the structure defines the possible node addresses and how they are organized, the source file only correlates none ambiguous names to those node addresses.
The diktataograph window is inspecting the datum, `1-1-2` (or rather amiguously named "msn-SAMRAS"), with respect to its structural facet (having already established that the focus is that datum, further depth negation is into its defined space rather than any further sub components.)
Now I want the tool to default to that navigation to be on on summit county ohio (`3-2-3-17-77`). This is so that the top right side section can visualize the associated profile.
This can be understood as the state machine's mediation on address node of the current navigation in the msn SAMRAS datum's strucure-id-space WITH RESPECT TO the associated profile source file.
e.g. Med(3-2-3-17-77-1);(sc.3-2-3-17-77-1-6-4-1-4.fnd.3-2-3-17-77-1-1.json).
Therefore its not like these sections are doing mediation operation at the same time, its rather that one is displaying the resulting visualization of the mediation directive that it operates with in the background and freezing the view until use again. Although the use of the strucure section 'updates' the profile/geospacial section.
The profile brought in is meant to use the 'geojson lense' or other cache technique for lessening the load of having to load all the source files and intake them for visualization.
The goal is to have that section reflect a single coordinate fields that can load a porfiles spacial deffintion, then upon the use of the bottom section to update the depth of view, add the subsequent child profile for theire spacial values.
This should also help to round out and unfiy the understanding and implementation of NIMM-AITAS. (The NIMM directive model is how the state is updated, AITAS similarly is the context in which that state is understood.)
Attention: 3-2-3-17-77 (summit_county)
Intention: 3-2-3-17-77-0-0 (Targeted Depth ~ How the attention is being held): The "How": Render any node whose ID starts with 3-2-3-17-77- and has one or two additional segment. (e.g., city_of_akron 3-2-3-17-77-1-1, village_of_reminderville 3-2-3-17-77-3-9, summit_county_townships 3-2-3-17-77-2-0).
Time can still be eft alone.
achetype is set at the value of the Nominal strucure families, ergo the hyphae value of the msn-samras, the nominal structure families are what give the 'space' a navigation lgoic with out there being any actual datum needed to 'navigate'. So a similar mediation can be applied to chronological HOPS and spacial HOPS.

More technical version of the notes:

`CTS-GIS` remains the `system.tools.cts_gis` `tool_mediation_surface` under `SYSTEM`. Its default posture is `interface-panel-led`: the dominant `Interface Panel` is the primary tool surface, and the `workbench` stays `tool_secondary_evidence` with `visible=false` until the runtime explicitly requests secondary evidence. The runtime already describes `CTS-GIS` as a `Spatial mediation surface owned by SYSTEM`, with the inspector populated as a `tool_mediation_panel`.  
Inside that dominant `tool_mediation_panel`, describe one CTS-GIS-local interface body. The shared shell names stay unchanged: `Interface Panel`, `tool_mediation_panel`, and `tool_secondary_evidence`. The tool-local section titles may then be `Diktataograph` for the left section and `Garland` for the right section. Those two names should be treated as CTS-GIS section labels, not as shell-region names. I do not see either term as an existing shared repo noun.
Use this structure statement: `The CTS-GIS tool_mediation_panel mounts one CTS-GIS-local interface body with two tool sections: Diktataograph and Garland. In narrow posture, the same CTS-GIS-local sections may fall back to a vertical stack.` I do not see a shared repo-wide two-pane CTS-GIS contract token yet, so this should be described as CTS-GIS-local interface content inside the existing `tool_mediation_panel`, not as a new shell layout type. The shared tool container names present in the UI layer are `system-tool-interface` and `system-tool-interface__body`. 
For the operation model, use this wording: `CTS-GIS does not change the shell contract when the user navigates inside a SAMRAS-defined structure. The shell mediation subject remains reducer-owned and file-backed. CTS-GIS then projects tool-local structural navigation inside that current mediation subject.` That stays aligned with the current shell contract, which only gives the ordered shell focus stack `sandbox -> file -> datum -> object`, and with the CTS-GIS runtime, which already returns `focus_subject` and `mediation_subject` as the active tool subject.   
For your source-file distinction, use this wording: `CTS-GIS mediates on the selected anchor-file datum and projects correlated source-file evidence into the Interface Panel. The supporting source file is not the primary mediation subject; it is supporting evidence used to resolve labels, profiles, and spatial definitions for the currently navigated structure address.` That is the safest phrasing against the current repo, because the CTS-GIS runtime already exposes datum-backed readiness and source-file availability through `datum_summary`, while the tool contract keeps the tool surface mediation-led rather than source-file-led. 
For datum `1-1-2`, use this wording: `CTS-GIS seeds its tool-local structural navigation from anchor-file datum 1-1-2 inside the canonical system anchor file.` Do not describe this as adding a new shell level below `object`. In current repo terms, that is a tool-local navigation state inside the current mediation subject, not a new shell focus segment. The only shared shell depth terms are `sandbox`, `file`, `datum`, and `object`.  
For the default county target, use this wording: `CTS-GIS defaults its tool-local navigation root to structure address 3-2-3-17-77, corresponding to summit_county, and projects the correlated profile evidence for that address into the Garland section.` Again, call this a `tool-local navigation root`, not a shell-level focus segment, because the shared shell contract does not currently define deeper address-space navigation terms for tool interiors.
For the section roles, use this wording.
`Diktataograph` is the CTS-GIS structural navigation section. It projects the navigable address-space defined by the active SAMRAS datum and may overlay correlated names from the supporting source document through the ASCII lens. Its updates are tool-local navigation updates within the current mediation subject.`
`Garland` is the CTS-GIS correlated profile section. It projects the current address node’s profile evidence and spatial definition with respect to the supporting source file. Its preferred loading posture is cached profile projection through the GeoJSON lens or equivalent runtime cache, so that CTS-GIS does not need to fully reload all supporting source files on each navigation step.`
That distinction preserves your intent: one section is structural navigation, the other is correlated profile projection. The second section updates when the first section changes the current tool-local navigation root.
For the relation between the two sections, use this wording: `The Diktataograph section drives CTS-GIS tool-local structural navigation. The Garland section projects the resulting correlated profile evidence for the currently navigated address node. These are not two independent mediation operations; they are two projections of one active CTS-GIS mediation posture.`
For the workbench rule, use this wording: `CTS-GIS stays interface-panel-led by default. Any workbench content remains secondary evidence and explicit opt-in.` That matches the current contract and runtime.  
For `Attention`, `Intention`, `Time`, `Archetype`, and `NIMM-AITAS`, I do not see those as shared shell/runtime nouns in the repo files reviewed. Use them only as CTS-GIS-local state labels, not as shell-contract terms. The safe phrasing is:
`Attention` → `CTS-GIS tool-local navigation root`
`Intention` → `CTS-GIS tool-local projection rule`
`Time` → `tool-local temporal directive`
`Archetype` → `tool-local structure-family directive`
```

### Q&A

How should CTS-GIS tool-local navigation state be carried in this pass?
Body-only: Keep shell URY/query unchanged and carry CTS-GIS local state like attention/intention in the tool requst/runtime payload first. This is meant to further flesh out a state machine alignment and consolidate lingering refference to the NIMM-AITAS models from past repo docs current docs and code. so the mediation scipt that operates as the directive is articulating the state chnage for the interface panel to represent. Therefore NIMM directives and AITAS context controls are to be controled from the control panel.
What should be the canonical source of the CTS-GIS SAMRAS seed for this plan?

Consider past repo updates that removed the use of `refferences/` and `resources/` to unify operations around only `payloads/` and `/payyloads/cache/`. It was then that the duel data locations were deffined for all tools, or at least meant to be. Tool sand boxes, like this one, should have convetionalized name for their anchor file like `tool.3-2-3-17-77-1-6-4-1-4.cts-gis.json`. This is for the tools use of the sandbox. However, none datum file are kept by a file in `private/utilities/tools/<tool>/spec.json`. Its in a tools `spec.json` that tool files can be managed, so the link is making sure that the tools anchor file has a datum in refference to the JSON-file-unit rudiment datum (`0-0-11`). This can be made the convention's expectation to find the spec.json refference at `1-0-1`. Prior to this, all refferences to the 'maps' tool were meant to be corrected to 'CTS-GIS', so this can be aligned along with all other tools to match a common convention.

### PLAN

- Normalize CTS-GIS around one canonical tool vocabulary: `CTS-GIS` / `cts_gis` / `cts-gis`, with legacy `maps` treated as compatibility-only.
- Keep the shared shell contract unchanged: `CTS-GIS` remains `system.tools.cts_gis`, `tool_mediation_panel` remains the dominant `Interface Panel` container, and `tool_secondary_evidence` remains the hidden-by-default workbench.
- Make CTS-GIS state body-carried, not URL-carried, in this pass. The shell query stays unchanged; CTS-GIS-local navigation and projection state is carried in the tool request/runtime payload and controlled from the `Control Panel`.
- Populate the dominant `tool_mediation_panel` with one CTS-GIS-local interface body:
  - left section: `Diktataograph`
  - right section: `Garland`
  - narrow posture: three-row vertical stack with a compact CTS-GIS context strip above `Diktataograph` and `Garland`

**Implementation Changes**

#### 1. Tool vocabulary and storage conventions
- Canonicalize all active repo docs, runtime labels, adapters, and tests to `CTS-GIS`; remove active public use of `maps` for this tool.
- Standardize CTS-GIS tool storage conventions:
  - tool root: `private/utilities/tools/cts-gis/spec.json`
  - tool sandbox anchor file: `data/sandbox/cts-gis/tool.<msn>.cts-gis.json`
  - tool anchor convention: datum `1-0-1` references `spec.json` through the JSON-file-unit rudiment `0-0-11`
- Make the datum store and CTS-GIS loaders prefer canonical `cts-gis` names first, but continue reading legacy `maps` names and paths as compatibility aliases until a later migration.
- Move CTS-GIS docs/tests away from `sandbox_source` as the public mental model. Public wording should describe:
  - tool anchor and tool spec as tool-owned files
  - payload JSON and payload cache JSON as the authoritative CTS-GIS evidence corpus

#### 2. CTS-GIS-local state model
- Keep shell-level focus unchanged: no new shell depth below `sandbox -> file -> datum -> object`.
- Add a CTS-GIS-local body-carried state object to `mycite.v2.portal.system.tools.cts_gis.request.v1`, accepted by both the direct tool endpoint and the shell-composed tool runtime:
  - `tool_state.nimm_directive`
  - `tool_state.aitas.attention_node_id`
  - `tool_state.aitas.intention_rule_id`
  - `tool_state.aitas.time_directive`
  - `tool_state.aitas.archetype_family_id`
  - `tool_state.source.attention_document_id`
  - `tool_state.selection.selected_row_address`
  - `tool_state.selection.selected_feature_id`
- Keep current CTS-GIS mediation keys working as compatibility aliases:
  - legacy `mediation_state.attention_node_id`
  - legacy `mediation_state.intention_token`
  - legacy `selected_row_address`
  - legacy `selected_feature_id`
- Normalize outward CTS-GIS runtime state to one canonical tool-local vocabulary:
  - `Attention` = current CTS-GIS tool-local navigation root
  - `Intention` = current CTS-GIS tool-local projection rule
  - `Time` = tool-local temporal directive, default empty/inactive
  - `Archetype` = tool-local structure-family directive, default `samras_nominal`
- Expand repo-wide docs/contracts around NIMM-AITAS vocabulary, but do not break the current shell-level `AitasContext` validator. Shared shell AITAS stays as-is; CTS-GIS gets an additive richer tool-local AITAS layer.

#### 3. CTS-GIS data flow and defaults
- Define CTS-GIS source precedence explicitly:
  - tool governance: `spec.json`
  - tool anchor: `tool.<msn>.cts-gis.json`
  - structural seed: registrar payload / payload cache
  - label evidence: administrative payload cache through the ASCII lens
  - spatial evidence: GeoJSON lens or equivalent cache derived from payload/payload-cache material
- Do not claim that `anthology.json:1-1-2` is the CTS-GIS SAMRAS seed in this pass. Instead:
  - CTS-GIS resolves its SAMRAS seed tool-locally from the CTS-GIS registrar/tool-anchor evidence
  - if the expected SAMRAS seed is absent there, CTS-GIS reports a readiness failure instead of silently falling back to unrelated anthology rows
- Set the CTS-GIS defaults explicitly:
  - seed structure family from the CTS-GIS SAMRAS registrar datum
  - default `attention_node_id = 3-2-3-17-77`
  - default `intention_rule_id = descendants_depth_1_or_2`
  - default `time_directive = ""`
  - default `archetype_family_id = samras_nominal`
  - default supporting source document for the Summit County corpus: `sc.3-2-3-17-77-1-6-4-1-4.msn-administrative.json`
- Define `descendants_depth_1_or_2` as:
  - render nodes whose ID starts with the current attention root
  - include descendants with one or two additional address segments
  - keep this as a CTS-GIS-local projection rule, not a shell focus rule

#### 4. Interface Panel and Control Panel behavior
- Keep `tool_mediation_panel` as the shared shell container, but give CTS-GIS a dedicated interface body payload and renderer:
  - `interface_body.kind = cts_gis_interface_body`
  - `layout = dual_section`
  - `narrow_layout = context_diktataograph_garland_stack`
- `Diktataograph` section:
  - structural navigation section
  - shows the active SAMRAS-defined address space rooted at the current attention node
  - overlays correlated names from the supporting administrative source via the ASCII lens
  - updates only CTS-GIS-local `Attention` and `Intention`
- `Garland` section:
  - correlated profile and spatial projection section
  - shows the current node’s profile evidence, selected row/feature, and GeoJSON-backed spatial definition
  - updates whenever `Diktataograph` changes the current CTS-GIS-local navigation root
- These two sections are projections of one active CTS-GIS mediation posture, not separate mediation operations.
- The `Control Panel` remains the place where CTS-GIS-local NIMM/AITAS controls live. Extend the existing `focus_selection_panel` content for CTS-GIS with:
  - directive summary/control group
  - AITAS group for Attention / Intention / Time / Archetype
  - source-evidence group for active tool anchor, spec, registrar, and administrative source selection
- Keep the `Workbench` as `tool_secondary_evidence` and explicit opt-in. When opened, it should show raw or diagnostic supporting evidence, not duplicate the primary Garland projection.

**Public Interfaces**
- No shell route changes and no shell query expansion in this pass.
- Additive CTS-GIS request changes:
  - `tool_state` object as the canonical CTS-GIS-local state carrier
  - legacy CTS-GIS mediation keys remain accepted
- Additive CTS-GIS runtime changes:
  - `surface_payload.tool_state`
  - `surface_payload.source_evidence`
  - `inspector.interface_body` for the CTS-GIS-local two-section body
  - legacy `mediation_state` remains readable during compatibility
- Additive terminology contract:
  - shared shell terms remain `Interface Panel`, `tool_mediation_panel`, `tool_secondary_evidence`
  - `Diktataograph` and `Garland` are CTS-GIS-local section labels only

**Test Plan**
- Runtime/unit:
  - CTS-GIS defaults to `attention_node_id=3-2-3-17-77`
  - default `intention_rule_id=descendants_depth_1_or_2`
  - CTS-GIS runtime normalizes legacy mediation keys into canonical `tool_state`
  - Garland projection changes when Diktataograph changes attention/intention
  - readiness failure is explicit when SAMRAS seed evidence is missing
- UI/static:
  - dedicated CTS-GIS inspector renderer mounts `cts_gis_interface_body`
  - full posture renders side-by-side Diktataograph/Garland
  - narrow posture renders the three-row stack
  - workbench remains hidden by default and stays secondary evidence
  - CTS-GIS control panel exposes tool-local directive/AITAS groups
- Adapter/data-contract:
  - canonical `cts-gis` anchor/spec naming is preferred
  - legacy `maps` path/tool-id/file-name inputs still load successfully
  - payload/payload-cache evidence is recognized as CTS-GIS source material
- Docs/contract:
  - repo docs prefer CTS-GIS over maps
  - NIMM-AITAS crosswalk is documented for tool-local state
  - docs clearly distinguish shell mediation subject from CTS-GIS-local structural navigation

**Assumptions and Defaults**
- This is a compatibility-preserving normalization pass, not a breaking shell-contract rewrite.
- Shared shell AITAS validation does not widen in this pass; the richer CTS-GIS AITAS vocabulary is additive and tool-local.
- Legacy `maps` data remains loadable until a later explicit migration removes it.
- The authoritative CTS-GIS structural/profile evidence comes from payload JSON and payload-cache JSON; the tool sandbox anchor and `spec.json` govern tool ownership and linkage.
- The current `anthology.json` contract remains unchanged; CTS-GIS SAMRAS seeding is treated as tool-local evidence resolution, not as a new shared SYSTEM anthology invariant.

## v2.5.3 Portal Shell Stabilization + Canonical Unification

```text
Now consider the images attached and consider the notes below for an audit of problem areas and the correction, aligment, and implementation of a more unified, sturdy, organized portal aplication.
(Consider revent updates in the repo as needed to under the direction of development in this v2.5.3 version. Most recently CTS-GIS was further developed, although the actual output can only be considered after making it viewable and not crammed.)


1.) In the Menu Bar, the control panel button in the header should not be any where in the code or docs. Additionally the Workbench button should not exist as it is now either. The only button of these kind should be parallels to the toggle button for the interface panel. Three side by side, left to right would be the icon buttons for the control panel, the workbench, and the interface panel. Hovering can provide a label. Seeing as how the exception was chosen to allow for tool to provide the workbench and the interface panel at the same time (rather than the default toggle of one, toggles off the other), double clicking can lock the appearance to hold this functionality. This should be the only avenue of this nature.

2.) The label and sub-header of each of the pages' Menu bar are made to fall off the visible principle section. all section should have well defined containment so that this isn't possible, rather it should be well designed so that the text follows to the right since the menu bar isn't meant to be any larger either. I want to make sure containers are well designed in this way, especially the principal containers (where the portal window does have any margins but still should be well defined to contain the subsequent activity bar, workbench and control panel. As well as the subsequent sections contain in each of those.)
3.) The work that was just recently done for the CTS-GIS tool's mediation, in the interface panel, is not at all viable for confirmation. This is the result of the interface panel's behavior not filling the width of the space provided, given that the work bench is now hidden. Therefore any changes must proceed after the containers are properly defined to be a stable component for content to be held in.
4.) The System page should default a landing page providing a workbench that shows the system sandbox. Showing the 'profile cards' that show the source section of source cards, and other root level datum files like anthology.json and system_logs.json (note that selecting system_logs.json navigates the portal's state to the network page). The control panel should reflect a lack of datums and no section are yet needed for further control at this state, on this page.

5.) Upon the navigating to the anthology.json from the sandbox attention, the attention is updated to be anthology.json focused. There in the control panel is idled for investigation directives. Similar to the 'mediation' with respect to the SAMRAS strucure, the control panel on the datum file, 'anthology.json' allows for investigation anthology to select layers, value-groups and iterations for by having the attention be on the anthology.json and the intention having values of `<layer>-<value_group>-<iteration>` (Time is null, archetype is the MSS structure of datum addressing, and space is null as-well). alternatively the control panel should allow for the selection change of the archetype value to select from 'MSS' or 'Hyphae'. The Hyphae would merely allow for the attention value to be selected for families or abstraction groups the data engine uses to group datums from their hyphae value. Historical repo updates may need to be examined for further context on this matter for how the Hanus data engine is meant to be organized and modularized in this new paradigm while enabling a clean modular implementation that align with this description. Opt for alignment to a unified Hanus data engine and its use of the NIMM-AITAS directive-context model.
- Current repo note: At current repo scope, anthology.json investigation belongs to the shared SYSTEM shell model, not to the CTS-GIS-local AITAS model. Once file=anthology, the Control Panel remains a focus_selection_panel that projects current context first and the selectable material below the current focus second. For anthology specifically, the repo currently renders a layered datum table grouped by layer and value_group, and datum rows carry structural coordinates layer, value_group, and iteration. That is the canonical current-repo language for anthology investigation.
- Requested canonical change: The repo-strict way to describe the requested expansion is: SYSTEM anthology investigation should remain file-backed and coordinate-driven, but may be widened to project an additional investigation directive model over the anchor file. This is not a requested extension of the current system-workspace contract, rather it is a investigation and alignmetn into the reailties and pattern seen in the the current paradigm materiaize a more clean canonical behavior. Trying to also include the mention of the NIMM-AITAS context for the system page was meant to show another context of use to claridy and help flesh out the pattern that is actually usable for a stable design. (AISTAS is the CONTEXT model for Attention, Intention, Time, Archetype and Space. NIMM is the directive model for Navigation, Investigation, Mediation, and Manipulation).
- Historical mirror from repo commits and current code: There is some mirrored basis for this in the repo, but only partially. The current code still carries datum-recognition fields such as primary_value_token, recognized_family, and recognized_anchor, and it already exposes a Copy Hyphae Value action when a selected datum has a primary value token. So Hyphae Value is present in current code as datum-recognition/action language. But I do not see current-repo support for making Attention/Intention/Time/Archetype/Space the canonical SYSTEM anthology model, I do not see a documented MSS versus Hyphae archetype selector in SYSTEM, and I do not see Hanus as a current canonical engine label in the repo files reviewed. In the current repo, NIMM/AITAS is explicitly tool-local to CTS-GIS, not the shared anthology contract. So the repo-strict mirrored formulation is: keep anthology in shared shell terms, and treat NIMM-AITAS-style directive/context expansion as a proposed future widening of the system-workspace model rather than as present canon.

6.) Fully unify the docs and code to have canonical term and object usage as best as possible. eliminating clutter/drift/confusion for a more clean and simple portal application.
- Current repo note: The canonical cleanup objective is vocabulary unification across current code, docs, templates, runtime bundles, and tests. The repo already moved in that direction by removing the old operational-status surface, replacing stacked_focus_panel with focus_selection_panel, making the activity bar icon-only, and defining one stable shell vocabulary around ide-shell, ide-menubar, ide-body, Control Panel, Workbench, Interface Panel, current context, below the current focus, interface-panel-led, and the shared tool contract.
- Requested canonical change: Docs and code should continue converging on one term set only, with obsolete or parallel labels removed wherever they still survive.
```

**Q&A**
* For v2.5.3, what should a fresh `/portal/system` load by default?
        * Anthology default (Recommended)
* How should the new three-icon toggle behavior work in normal vs locked mode?
        * This is a replacement of how to have the workbench be used at the same time as the interface panel, for tools. This is because by default tools provide the interface panel and hide the workbench, also by default the initial pass made it so they were mutually exclusive but alloted for an eception if desired. That exception is meant to be materialized in the double click so that toggling the workbench or interface panel on does does not subsequently toggle the other off. This does not apply to any other use other than for tools, there is not really a point to this as on a none tool page like the system page, each panel is indepdently toggled. The main concern is that the interface panel expands to fill the space provided with the workbench toggled off.
* What scope should we target for SYSTEM anthology in this release?
        * Semi current model + docs align: Keep layered file-backed anthology investigation canotical and document NIMM/AITAS expansion as future work. The exception is whether the unification of the terms and logic can simply be shifted toward this model without disruption, in that case try to unify the use of the terms and the operational meaning behind them.

### PLAN

**Summary**
- Replace the current menubar mixed controls with a single icon-toggle trio contract (`Control Panel`, `Workbench`, `Interface Panel`) and stabilize peer-region layout so panels always fill and contain content correctly.
- Keep SYSTEM canonical defaults as they are now (`/portal/system` seeds `file=anthology`) and keep anthology investigation on the shared layered/file-backed model.
- Apply a repo-wide terminology unification sweep with **soft migration** (canonical terms everywhere, legacy aliases retained with deprecation notes).

**Implementation Changes**
- **Menubar control model**
  - Convert header controls to three icon buttons only (with hover labels + accessible names).
  - Tool pages: `Workbench` and `Interface Panel` are mutually exclusive on single-click by default.
  - Tool pages: double-click on either `Workbench` or `Interface Panel` enables a route-scoped lock mode so enabling one does not auto-disable the other.
  - Non-tool pages: keep independent toggles (no enforced exclusivity logic).
  - Remove legacy “text-button” header implementation/docs language and standardize on icon-toggle wording.
- **Containment and width stability**
  - Normalize shell/body/panel container constraints (`min-width: 0`, bounded overflow, explicit grid/flex containment) so titles/subheaders do not spill outside principal regions.
  - Fix the inspector/interface-panel width bug when workbench is collapsed by removing fixed-width behavior in expanded-track states and forcing fill-to-track behavior.
  - Ensure CTS-GIS interface panel content uses full available width before any additional mediation UI refinement.
- **SYSTEM + anthology behavior**
  - Keep fresh SYSTEM state anchored to `file=anthology`.
  - Preserve current anthology control-panel behavior (`focus_selection_panel`, current context first; selectable material second).
  - Preserve coordinate-driven anthology investigation (`layer`, `value_group`, `iteration`) and existing hyphae-recognition actions; no shared-shell archetype selector in this release.
  - Keep sandbox cards behavior for sandbox-focused state and maintain `system_log.json` handoff to Network view.
- **Repo-wide canonical vocabulary sweep**
  - Build a single glossary mapping old/parallel terms to canonical terms.
  - Update code/docs/templates/runtime bundles/tests to canonical terms.
  - Retain legacy aliases/parsing shims where they affect compatibility; mark as deprecated and document removal target.

**Public Interfaces / Contracts**
- **DOM/UI contract**
  - Header control contract becomes icon-only trio with tooltip labels.
  - Add explicit lock-state signal for tool peer region behavior (route-scoped, not global).
- **Event/state contract**
  - Keep existing toggle request events; extend with lock-state handling for tool surfaces.
  - Keep reducer/shared shell contracts for SYSTEM anthology unchanged in v2.5.3.
- **Documentation contract**
  - Canonical shell/tool vocabulary becomes source-of-truth; legacy terms remain only in deprecation mapping notes.

**Test Plan**
- **Unit**
  - Toggle behavior matrix: tool vs non-tool routes, single-click exclusivity, double-click lock, lock-off behavior.
  - Inspector width/track behavior when workbench collapses/expands.
  - SYSTEM runtime defaults remain `file=anthology`; anthology coordinates remain intact.
- **Integration**
  - Header renders only icon toggles with proper labels/aria.
  - CTS-GIS panel fills width when workbench hidden; no crammed-column regression.
  - `system_log.json` selection still routes to Network logs view.
- **Repo consistency checks**
  - Contract/architecture/test fixtures updated to canonical terms.
  - Deprecation aliases still accepted where required for backward compatibility.

**Assumptions and Defaults**
- `/portal/system` default stays anthology-focused in v2.5.3.
- Anthology remains shared-shell, file-backed, coordinate-driven; NIMM/AITAS expansion is documented as future widening, not current canon.
- Double-click lock is tool-surface behavior only and route-scoped (not global persistent mode).
- Vocabulary cleanup is repo-wide, but migration is soft (no hard breaking alias removal in this release).

---
## v2.5.4 CTS-GIS Dual-Section Materialization (Mockup-Aligned)

- `CTS-GIS` remains `system.tools.cts_gis`, `interface-panel-led`, and shell-contract-stable.
- The dominant `tool_mediation_panel` now carries a CTS-GIS-local dual-section interface body contract:
  - `navigation_canvas` (`Diktataograph`) for anchored-path structural navigation.
  - `garland_split_projection` (`Garland`) for correlated projection with dominant `geospatial_projection` and secondary `profile_projection`.
- `CTS-GIS Context` is compact orientation context only, not a primary card section.
- `Workbench` remains `tool_secondary_evidence`, non-primary, and behaviorally unchanged.
- Narrow posture remains canonical stack: compact context strip, then Diktataograph, then Garland.
- Progressive enhancement policy: hover/attention redistribution for the structural canvas is feature-flagged and non-baseline in this release.

