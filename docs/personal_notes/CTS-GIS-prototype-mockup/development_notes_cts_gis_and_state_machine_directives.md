# CTS-GIS & TOOL CONVENTION/STATE MACHINE DIRECTIVES

## PLAN

The plan is to eventually have the garland section be made 3/5 of the mediation window and to shrink the content of the Diktataograph section, then I will plan to have the Garland section's Geospatial Projection be styled as being on top of the Profile Projection, and removing the Garland section's Current Profile. This plan would use this as the setting to have the Profile Projection sub-section contain a more visualy organize 'summary' objects. One of which would be a allow for toggling presently visualized aspects of the Geospatial Projection. Remember that the `Geospatial Projection` is a reflection of the state machine `mediation` on the "msn-SAMRAS" deffined strucure (datum addressed at`1-1-2` in the tools sandbox anchor file  @repo/mycite-core/deployed/fnd/data/sandbox/cts-gis/tool.3-2-3-17-77-1-6-4-1-4.cts-gis.json), the state machine is further deffined by having the deffinning the `Atention` value that determine the namespace address node that is directed to inside the 'ficticiouse' namespace that is implied by the msn-SAMRAS strucure's deffinning in the datums 'magnitude value'. The intion value merely deffines the depth of an attention values scrope, there in an intention value of `0-0` pulls into scope the children node addresses of the current node address up to a depth of 2 layer below it. Then finnaly the added feture is further deffining how the `time` context value is used. Unlike the `attention` context value (not necissarily having a devoted use for a particular type of datum, only how it allows for navigating deffined spaces) the `time` context value is aplicable for the state machine if and in the cirmustance that information objects contain the use of abstracted 'HOPS chronological' address nodes, it dosn't need to be a particular hyphae valued datum and only from the hyphae value deffined family of chronological spaces.
So here I want you to notice that the actual statemachine work is largly handed off to the tool, only have the state machine need to operate by having the the state be deffined as default as:
[`mediation`;(`CTS-GIS`.`tool.3-2-3-17-77-1-6-4-1-4.cts-gis.json`.`1-1-2`.`3-2-3-17`.`<1>`.`<today~4-447-751-507-818>`.`<SAMRAS_family>`.`4`:`CTS-GIS`]
With the directive being represented by convention as:
[`<directive>`;(`<sandbox_if_aplicable>`.`<file_if_aplicable>`.`<datum_address_if_aplicable>`.`<attention>`.`<intention>`.`<time>`.`<archetype>`.`<space>`:`target_mediation_in_respect_to`]
(ideally I would like this to be a polished script language so that what ever tool or directive interface surface uses it, things can be made more strucured and deffined, even with a CLI program interface it could use the same interface surface for attachment)
Here we observer that the mediation is made with resepect to the sandbox as a whole, that's akin to mediating on a subject with respect to another object. So the CTS-GIS tool's real purpose is to provide the visual interface that reflects the state (lightl updating it although this should be the job of the control panel, but first I must remove all the uneeded information.), but then also has the specal use of making since of visualizing the mediation. Since the product of mediating on a state with respect to an entire sandbox is not exactly a given. Thats becuase mediation results are what ever a tool chooses to do with the mediation equation. (Another example might be the mediation on a table data set with respect to a convertion table and the tool could provide an interface for what information migth convert to, but thats more complex. mediation is open ended in that way). Here were are using the state of medation on the msn-SAMRAS to pull in select information.
This is the explanation of how things should work, alignment to this would proceed the use of the CTS-GIS tool to provide a garland section that allows for additional precint projection. although currently, having it reflect a chnage in intention value is already prooving diffult. 

---